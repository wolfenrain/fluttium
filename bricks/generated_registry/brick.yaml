name: generated_registry
description: A brick for generating a Fluttium registry.
version: 0.1.0+1

environment:
  mason: ">=0.1.0-dev.26 <0.1.0"

vars:
  actions:
    type: string
    description: The actions to register
    prompt: What are the actions to register (encoded as a list)?
